# Problem Detection and Prioritization

After the matching pipeline completes ([Section 2](2.%20Matching%20process.md)), the system performs a comprehensive analysis to identify data quality issues in both datasets.

## Problem Categories

The system detects three categories of problems:

| Category | Level | Question | User Action |
|----------|-------|----------|-------------|
| **[Stop Problems](3.1%20Stop%20Problems.md)** |  **Node** | "Is this stop at the correct location with correct attributes?" | Move node, update tags, fix matching. |
| **[Route Entity Problems](3.2%20Route%20Entity%20Problems.md)** | **Global** | "Does this route exist and is it defined correctly?" | Create or delete route relation; fix route tags. |
| **[Route Membership Problems](3.3%20Route%20Membership%20Problems.md)** | **Relational** | "Is the list of stops for this route consistent?" | Add/remove stops from relation; reorder stops; fix roles. |

### 1. Stop Problems (Implemented)
Issues with **individual stops** (Distance, Attributes, Isolation, Duplicates). See [3.1 Stop Problems](3.1%20Stop%20Problems.md).

### 2. Route Entity Problems (Planned)
Issues with the **Route object** itself.
*   **Goal**: Ensure every ATLAS route has a corresponding OSM route with correct global metadata.
*   See [3.2 Route Entity Problems](3.2%20Route%20Entity%20Problems.md).

### 3. Route Membership Problems (Planned)
Issues with the **stop-route relationship**.
*   **Goal**: Ensure the sequence of stops and their directional roles match.
*   See [3.3 Route Membership Problems](3.3%20Route%20Membership%20Problems.md).

---

## Architecture

Problem detection is built on the same domain models as the matching pipeline. The `PipelineResult` (containing `MatchRecord`, `AtlasNode`, and `OsmNode` entities) flows directly into problem detection — no ORM or dictionary conversion is needed.

### Polymorphic Predicates

Each problem predicate is a plain function with a **polymorphic signature**:

```python
def predicate(ctx: ProblemContext, record: MatchRecord | AtlasNode | OsmNode) -> list[ProblemResult]
```

Predicates use `isinstance` checks to decide what to evaluate:
- `distance_problem` and `attributes_problem` only act on `MatchRecord` (return `[]` for bare nodes)
- `unmatched_problem` only acts on bare `AtlasNode` or `OsmNode` (returns `[]` for `MatchRecord`)
- `duplicates_problem` acts on all three types

This allows the same `STOP_PROBLEM_PIPELINE` list to be used for both matched and unmatched records.

### Two Invocation Paths

| Record type | Invocation | Where |
|-------------|-----------|-------|
| **Matched** (`MatchRecord`) | `match_record.evaluate_problems(problem_ctx, STOP_PROBLEM_PIPELINE)` | `importer.py` — calls the method natively on the domain entity |
| **Unmatched** (`AtlasNode` / `OsmNode`) | `run_problem_pipeline(STOP_PROBLEM_PIPELINE, problem_ctx, node)` | `importer.py` — uses the standalone runner |

Both paths produce `list[ProblemResult]`, which is then mapped to ORM `Problem` rows via `apply_problem_results()`.

### `ProblemResult` Value Object

All predicates return `ProblemResult`, a lightweight frozen dataclass decoupled from SQLAlchemy:

```python
@dataclass(frozen=True)
class ProblemResult:
    problem_type: str        # 'distance', 'attributes', 'unmatched', 'duplicates'
    priority: int            # 1 = P1, 2 = P2, 3 = P3
    has_atlas_duplicate: bool = False
    has_osm_duplicate: bool = False
```

---

## Priority Levels

All problems use a consistent three-level priority system:

| Level | Meaning |
|-------|---------|
| **P1** | Critical |
| **P2** | Significant |
| **P3** | Minor |

Priority assignment is **rule-based** and considers factors like:
- Distance thresholds (configurable constants in `context.py`)
- Operator type (e.g., SBB railway platforms have higher distance tolerance)
- Attribute importance (UIC references are more critical than operator names)
- Isolation status (stops with no nearby counterparts are higher priority)

---

## Problem Resolution and Persistence

The current problem-detection pipeline is **read-only**: it creates `Problem` rows with `problem_type` and `priority`, but it does not persist problem solutions or reapply them during import.

The API layer already exposes placeholder `solution` and `is_persistent` fields in some responses, and the split-database migration defines `persistent_data` and `user_notes` tables in `user_input_db`. However, those persistence tables are not currently wired into the active importer or Flask-SQLAlchemy models used by the web app.

At the moment, the implemented persistence behavior is limited to the architectural split itself: the import pipeline writes reproducible data to `import_db`, while `user_input_db` is provisioned separately for future auth and user-generated data flows. See [4. Databases](4.%20Databases.md) for the current state.

---

## Code Reference

| Component | File | Purpose |
|-----------|------|---------|
| Result value object | [problem_detection/result.py](../matching_and_import_db/problem_detection/result.py) | `ProblemResult` — frozen dataclass |
| Shared context | [problem_detection/context.py](../matching_and_import_db/problem_detection/context.py) | `ProblemContext.build()` — KDTrees, UIC counts, duplicate maps |
| Pipeline runner | [problem_detection/pipeline.py](../matching_and_import_db/problem_detection/pipeline.py) | `run_problem_pipeline()`, `STOP_PROBLEM_PIPELINE` |
| Distance predicate | [predicates/distance.py](../matching_and_import_db/problem_detection/predicates/distance.py) | `distance_problem()` |
| Attributes predicate | [predicates/attributes.py](../matching_and_import_db/problem_detection/predicates/attributes.py) | `attributes_problem()` |
| Unmatched predicate | [predicates/unmatched.py](../matching_and_import_db/problem_detection/predicates/unmatched.py) | `unmatched_problem()` |
| Duplicates predicate | [predicates/duplicates.py](../matching_and_import_db/problem_detection/predicates/duplicates.py) | `duplicates_problem()` |
| Domain models | [models.py](../matching_and_import_db/models.py) | `MatchRecord.evaluate_problems()`, `AtlasNode`, `OsmNode` |
| Database import | [database/importer.py](../matching_and_import_db/database/importer.py) | Calls problem detection per stop during insertion |
| API endpoints | [problems.py](../backend/blueprints/problems.py) | Problem listing and resolution |
