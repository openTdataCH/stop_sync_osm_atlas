services:
  redis:
    image: redis:7-alpine
    container_name: stop_sync_osm_atlas_redis
    restart: "no"
    command: [ "redis-server", "--save", "", "--appendonly", "no" ]
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 3

  db:
    image: postgis/postgis:16-3.4
    container_name: stop_sync_osm_atlas_db
    restart: "no"
    environment:
      POSTGRES_DB: import_db
      POSTGRES_USER: stops_user
      POSTGRES_PASSWORD: 1234
    ports:
      - "5432:5432" # Exposes Postgres to your host machine on port 5432
    volumes:
      - postgres_data:/var/lib/postgresql/data
      # Create user_input_db on first init
      - ./docker/postgres-init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB} -h localhost -p 5432" ]
      interval: 10s
      timeout: 5s
      retries: 2

  migrator:
    build:
      context: .
      target: app-stage
      args:
        APP_UID: ${APP_UID:-1000}
        APP_GID: ${APP_GID:-1000}
    container_name: stop_sync_osm_atlas_migrator
    restart: "no"
    entrypoint: ["/bin/bash", "-lc"]
    command: "python matching_and_import_db/database/init.py && flask db upgrade"
    volumes:
      - .:/app
      - ./data:/app/data
    depends_on:
      db:
        condition: service_healthy
    environment:
      FLASK_APP: backend/app.py
      DATABASE_URI: ${DATABASE_URI:-postgresql+psycopg://stops_user:1234@db:5432/import_db}
      USER_INPUT_DATABASE_URI: ${USER_INPUT_DATABASE_URI:-postgresql+psycopg://stops_user:1234@db:5432/user_input_db}
      RATELIMIT_STORAGE_URI: memory://

  app:
    build:
      context: .
      target: app-stage
      args:
        APP_UID: ${APP_UID:-1000}
        APP_GID: ${APP_GID:-1000}
    container_name: stop_sync_osm_atlas_app
    restart: "no"
    ports:
      - "5001:5001"
    volumes:
      - .:/app # Mounts your project directory into the container for live code changes
      # Mount data directory for persistence
      - ./data:/app/data
    depends_on:
      db:
        condition: service_healthy # Waits for db to be healthy before starting app
      redis:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
    environment:
      # Flask environment variables (some are also in Dockerfile for explicitness)
      FLASK_APP: backend/app.py
      FLASK_ENV: development # Or production
      FLASK_DEBUG: 1 # Enables Flask debugger
      # Database URI for SQLAlchemy, now configurable via .env
      DATABASE_URI: ${DATABASE_URI:-postgresql+psycopg://stops_user:1234@db:5432/import_db}
      USER_INPUT_DATABASE_URI: ${USER_INPUT_DATABASE_URI:-postgresql+psycopg://stops_user:1234@db:5432/user_input_db}
      SECRET_KEY: ${SECRET_KEY:-dev-insecure}
      FORCE_HTTPS: "false"
      SESSION_COOKIE_SECURE: "false"
      AUTO_MIGRATE: "false"
      RATELIMIT_STORAGE_URI: ${RATELIMIT_STORAGE_URI:-redis://redis:6379/0}
      RUN_STARTUP_PIPELINE: "${RUN_STARTUP_PIPELINE:-false}" # Optional one-shot run on app boot
    cpu_shares: 2048 # Higher CPU priority during contention
    mem_limit: 2g # Web app stays lighter, pipeline runs in scheduler service
    # mem_reservation: 1g # Optional: guarantee 1g

  test:
    build:
      context: .
      target: test-stage
      args:
        APP_UID: ${APP_UID:-1000}
        APP_GID: ${APP_GID:-1000}
    container_name: stop_sync_osm_atlas_test
    restart: "no"
    volumes:
      - .:/app
      - ./data:/app/data
    environment:
      DATABASE_URI: ${DATABASE_URI:-postgresql+psycopg://stops_user:1234@db:5432/import_db}
      USER_INPUT_DATABASE_URI: ${USER_INPUT_DATABASE_URI:-postgresql+psycopg://stops_user:1234@db:5432/user_input_db}
      RATELIMIT_STORAGE_URI: ${RATELIMIT_STORAGE_URI:-redis://redis:6379/0}

  scheduler:
    build:
      context: .
      target: scheduler-stage
      args:
        APP_UID: ${APP_UID:-1000}
        APP_GID: ${APP_GID:-1000}
    container_name: stop_sync_osm_atlas_scheduler
    restart: "no"
    command: ["scheduler"]
    volumes:
      - .:/app
      - ./data:/app/data
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
    environment:
      DATABASE_URI: ${DATABASE_URI:-postgresql+psycopg://stops_user:1234@db:5432/import_db}
      USER_INPUT_DATABASE_URI: ${USER_INPUT_DATABASE_URI:-postgresql+psycopg://stops_user:1234@db:5432/user_input_db}
      RATELIMIT_STORAGE_URI: ${RATELIMIT_STORAGE_URI:-redis://redis:6379/0}
      PIPELINE_TIMEZONE: ${PIPELINE_TIMEZONE:-Europe/Zurich}
      PIPELINE_SCHEDULE_HOUR: ${PIPELINE_SCHEDULE_HOUR:-2}
      PIPELINE_SCHEDULE_MINUTE: ${PIPELINE_SCHEDULE_MINUTE:-0}
      PIPELINE_IMPORT_ETA_SECONDS: ${PIPELINE_IMPORT_ETA_SECONDS:-280}
      AUTO_MIGRATE: "false"
    cpu_shares: 2048
    mem_limit: 4g

volumes:
  postgres_data: # Persists Postgres data
